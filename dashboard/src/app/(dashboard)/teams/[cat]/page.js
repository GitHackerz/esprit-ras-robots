import TableTeams from '@/components/Tables/TableTeams'

const Page = ({params}) => {
    console.log(params)
    return (
        <main>
            <TableTeams />
        </main>
    )
}

export default Page
